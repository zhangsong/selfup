# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

module:testrunner

GET     /                                       App.Index

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath                       Static.Serve("public")
GET 	/captcha/*filepath				Captcha.Index
GET 	/members/delete/:username				Members.Delete
GET 	/element/delete/:id				Element.Delete
GET 	/shijuan/ask/:id				Shijuan.Ask
GET 	/share/to/:id				Share.To
GET 	/lianxiang/:id/:type				Lianxiang.Index
GET 	/lianxiang/:id				Lianxiang.Show

# Catch all
*       /:controller/:action                    :controller.:action
